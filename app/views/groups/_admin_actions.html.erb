<% if current_user == @group.admin || @group.description.present? %>
  <div class='alert-message block-message warning'>
    <a class="close" href="#">×</a>
    <p>
      <% unless @group.description.blank? %>
        <div>
          <b>Group Info:</b> <%= @group.description %>
        </div>
      <% end %>
      <b> Invite Url: </b> <%= link_to new_user_url(:group_code => @group.code), new_user_url(:group_code => @group.code) %>
    </p>
  <br/>
  <% if current_user == @group.admin %>
  <div class="alert-actions">
    <%= link_to("Delete this group", group_path(@group), :method => :delete, :confirm => "Are you sure you want to delete this group?", :class => 'btn small') %>
  </div>
  <% end %>
  </div>
<% end %>
