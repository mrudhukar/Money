<% @title = "Reset Password" %>

<%= form_for @user, :url => reset_password_url, :validate => true  do |f| %>
	<%= f.password_field :password %>
	<%= f.password_field :password_confirmation %>
	<%= f.submit "Reset Password and Log in" %>
<% end %>
